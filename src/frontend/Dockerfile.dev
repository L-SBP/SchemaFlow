# ./src/frontend/Dockerfile.dev
# 使用满足 Vite 要求的 Node 版本（20.x+）
FROM node:20-alpine

WORKDIR /app

# 安装依赖（利用 Docker 层缓存）
COPY package*.json ./
RUN npm ci --silent

# 启动 Vite 开发服务器
CMD ["npm", "run", "dev"]